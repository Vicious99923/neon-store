## Packages
framer-motion | Complex animations and page transitions
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind CSS classes safely

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  sans: ["Inter", "sans-serif"],
  display: ["Space Grotesk", "sans-serif"],
}
Cart state is managed in localStorage via use-cart hook.
Auth is handled by Replit Auth (useAuth hook).
Images use Unsplash placeholders.
